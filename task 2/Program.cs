using System.Diagnostics;
using System.Reflection.Metadata;

namespace task_2
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region task 2 documentation
            //2 - Producer - Consumer Problem: Implement the producer - consumer scenario using threads,
            //    where producer threads create data items and place them in a buffer,
            //    and consumer threads remove and process those items.
            //    Use synchronization mechanisms to handle the access to the shared buffer. 
            #endregion
        }
    }
}
